# rates.html — 「온체인 라이브」 허위 표시 수정 (2026-08-12)

## 요약
2개월 된 seed 상수가 「온체인 라이브」 헤더 아래 그려지던 결함을, **아카이브(과거 블록) 델타로
첫 페인트에 실측**하도록 근본 수정. seed는 아카이브 실패 시 폴백으로만 남고, 그 상태가
헤더·행 배지로 화면에 드러난다. 수정 파일 = `rates.html` 하나(120+/67−).

## 무엇을 고쳤나

### 1. 아카이브 델타 (근본 수정)
- 신규 `onOne(chain,fn)` — **한 요청 묶음은 반드시 같은 RPC 엔드포인트**에서 처리.
  기존 `call`/`storage`는 슬롯마다 독립적으로 엔드포인트를 골라, 아카이브 조회 시
  gi는 과거 노드·ep는 latest 노드에서 오는 조용한 오염이 가능했다.
- 신규 `clk(chain)` — 블록시간 **실측**(하드코딩 금지, 1000블록 창). 새로고침마다 캐시 폐기.
- 신규 `archiveDelta(chain,at,now)` — 8·16·32·64h 후보 블록을 순차로 물러나며 조회.
  **시간은 과거 블록 후보를 고르는 힌트일 뿐이고, 율의 분모는 항상 온체인 epoch 카운터 Δ.**
  가드: `prev.ep>0`(프루닝 노드가 0을 준 경우 배제), `1≤Δep≤24`(Δep=0 = 아카이브 미지원 →
  더 멀리 재시도 → 전부 실패면 null 반환 → 호출자 폴백).
- `readRebaseAnu()` — 오라클 `0xb4E5b766…`의 `currentIndex()(0x26987b60)`·`currentEpoch()(0x76671808)`를
  현재/과거 두 시점에서 읽어 `perEpIdx = (idx_now/idx_prev)^(1/Δep) − 1` (복리 → 기하).
  기존의 slgns `index()` 경로는 제거(오라클 직독으로 통일).
- `readExtraPoly`+`readExtraAnu` → **`readExtra(chain,prod)` 하나로 통합**(구조가 동일했다).
  `perEpGi = Δgi/Δep/denom×100` (단리 → 산술). Polygon slot 0x6a/0x6b·denom 1e9,
  Anubis slot 0x6b/0x6c·denom slot 0x71. **Anubis도 아카이브 `eth_getStorageAt`을 지원**함을
  실측 확인해 4개 상품 전부 아카이브 경로를 탄다.
- 폴백은 기존 localStorage 델타 그대로 유지(코드 경로만 뒤로 밀림).
- 죽은 코드 `rateFromAcc()` 삭제(호출부 없음).

### 2. 상태 태그 전파 (행 ③④)
`renderChain`에서 `dstat=[reb.status,ex.status].filter(s=>s!=='live').sort()[0]` →
①② 중 하나라도 비-live면 ③(일일 합산)과 **④(가장 눈에 띄는 1년 숫자)** 에도 배지를 단다.

### 3. 배지 상태 (Sell Tax·Turbine Fee)
`setBadge(...,st)` 인자 추가. `go()`가 성공한 read에만 `taxSt/turbSt='live'`를 세우고,
`seedState()`는 'seed'로 시작 → RPC 실패 시 시드값이 라이브로 오독되지 않는다.

### 4. 헤더 문구
`<span id="liveLbl">` 신설 + `renderHdr()`. 전 항목 live면 `온체인 라이브`,
아니면 `⚠ N항목 기준값(미실측)`(주황) + 툴팁으로 폴백 사실 안내.

### 5. seed 상수 갱신 (2026-08-12 온체인 재측정)
| 키 | 기존 | 갱신 |
|---|---|---|
| rebaseEp.poly / .anu | 0.1499 / 0.1495 | **0.1308 / 0.1308** |
| rebaseDay.poly / .anu | 0.5994 / 0.5981 | **0.5232 / 0.5232** |
| extraEp.p360 / p600 | 0.1200 / 0.0783 | **0.0800 / 0.0599** |
| extraDay.p360 / p600 | 0.2400 / 0.1566 | **0.1600 / 0.1198** |
| sellTax.anu | 0.2875 | **0.3825** |
(anu extra 0.15/0.195는 현재값과 일치 → 유지)

### 6. 600일 기간 표기
`term:"25,920,000blk ≈600d"` → `"51,840,000s ≈600d"`.
근거: `0x8cA97F41…` `termSeconds()(0x48f1cd7b)` = **51,840,000** (실측, 아래 로그).
p360 `termSeconds()` = 31,104,000 → 기존 표기 정확(무수정).

### 손대지 않은 것 (감사에서 정확 확인됨)
`getReserves` 직독·토큰 순서 반전, Polygon 리베이스 즉시 직독(`epoch()` word3 ÷ circulating),
매도세 곱셈 합성, `epoch().end`=블록번호 해석, 근거(evidence) 패널, index.html 전체.

## 실측 로그

### 아카이브 지원 확인 (node, 2026-08-12)
```
anubis blk 10948079
back 40000 idx 986.339587904 ep 2603
back 80000 idx 983.740114696 ep 2601
now  idx 988.933949872 ep 2605
poly blk 91847390
p360 now gi 255600000 ep 2436
  p360 back 40000 gi 254000000 ep 2434 dep 2 perEp% 0.08000
p600 now gi 583660000 ep 2436
  p600 back 40000 gi 582462000 ep 2434 dep 2 perEp% 0.05990
a360 now gi 302500000 ep 2436 denom 1000000000
  back 100000 gi 298000000 ep 2433 dep 3 perEp% 0.15000
a600 now gi 389350000 ep 2436
  back 100000 gi 383500000 ep 2433 dep 3 perEp% 0.19500
p360 termSeconds 31104000
p600 termSeconds 51840000
poly blocktime 1.5   anu blocktime 1
```

### 검증 ① 첫 페인트 (localStorage 전체 삭제 + SW/캐시 해제 후 로드, 대기 0)
헤더: `온체인 라이브` · `P#91847692 · A#10948535`

Polygon 360일 LONG
```
① 일반 리베이스 (복리)   0.5234%/day [온체인Δ]  0.1308%/ep·6h
② Extra interest (단리)  0.1600%/day [온체인Δ]  0.0800%/ep·12h
③ 일일 합산 (①+②)       0.6834%/day
④ 단순 계산 한 달·1년     +21.8% · +633%
```
Polygon 600일 LONG
```
② Extra interest (단리)  0.1198%/day [온체인Δ]  0.0599%/ep·12h
④ 단순 계산             +20.6% · +618%
600일 기간 표기          51,840,000s ≈600d
```
Anubis 360/600
```
① 일반 리베이스          0.5232%/day [온체인Δ] 0.1308%/ep·6h
② 360 0.3000%/day [온체인Δ] 0.1500%/ep · 600 0.3900%/day [온체인Δ] 0.1950%/ep
④ 360 +26.0% · +684%   600 +28.7% · +717%
배지  Sell Tax 5.00% / 38.25% · Turbine Fee 0.30% / 5.32% (전부 live → 배지 없음)
```
→ 목표치(Anubis 리베이스 0.1308 · p360 0.0800 · p600 0.0599) **첫 페인트에 일치**.

### 검증 ② 행 ④
Polygon 360일 1년 = **+633%** (수정 전 +878%). Anubis 360일 = +684% (수정 전 +895%).

### 검증 ③ 아카이브 실패 주입
`fetch`를 감싸 블록 파라미터가 `latest`가 아닌 요청만 거부하고 `go()` 재실행:
```
헤더:  ⚠ 5항목 기준값(미실측)      ← anu 리베이스 + extra 4종
② Extra interest 0.1600%/day [기준값] 0.0800%/ep
③ 일일 합산      0.6834%/day [기준값]
④ 단순 계산      +21.8% · +633% [기준값]     ← 큰 숫자에 경고 전파됨
① Polygon 리베이스 0.5234%/day [온체인Δ]      ← 즉시 직독이라 정상 live 유지
```
→ 폴백 상태가 헤더·③·④ 모두에 드러남.

### 검증 ④ index.html 회귀
`git status` = `M rates.html` 1개(index.html 무접촉). 로드 정상 —
Polygon $1.49/Anubis $2.07, 매도세 5.00%/38.25%, 원화·환율·메이저 코인 전부 채워짐.
차트 영역이 비어 보이는 것은 DEXTools **외부 iframe**이 이 브라우저 환경에서 로드되지
않는 것으로, 수정과 무관하며 기존 동작.

### 자체 테스트
콘솔 `✅ self-tests passed`. 신규 3항 추가(2026-08-12 스냅샷 앵커):
`perEpGi(p360)=0.0800` · `perEpGi(p600)=0.0599` · `perEpIdx(anu, Δep=4)≈0.1316`.

## 우려 / 남은 것
- **Δep 상한 24 하드캡**: 아누비스가 64h(=Δep 10) 뒤에도 epoch 전환이 없으면 폴백으로 떨어진다.
  현 관측(6h/12h epoch)에서는 8h 첫 후보로 대부분 성사.
- **RPC 호출 증가**: 새로고침당 체인별 블록클럭 3콜 + 상품별 아카이브 1~4회 왕복.
  공개 RPC 레이트리밋에 걸리면 폴백(=기준값 배지)으로 조용히 내려앉는다 — 화면엔 드러남.
- **seed는 여전히 늙는다**. 아카이브가 살아 있는 한 안 쓰이지만, 폴백으로 남는 이상
  주기적 갱신 대상. `sellTax.anu`는 운영자 가변 레버라 특히 빨리 낡는다.
- 배포·push는 하지 않았다(로컬 커밋까지). 검증용 정적 서버(`python3 -m http.server 8911`,
  pid 4154)는 **자기 프로세스만** 종료 확인(:8911 리스너 0). 8899를 쓰던 타 세션 서버는 무접촉.
